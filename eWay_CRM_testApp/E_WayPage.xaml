<UserControl x:Class="eWay_CRM_testApp.E_Way"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:eWay_CRM_testApp"
             Padding="10" Background="Blue"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local1="clr-namespace:eWay_CRM_testApp"
             mc:Ignorable="d">

    <UserControl.Resources>
        <local:PhotoPathToUriConverter x:Key="PhotoPathToUriConverter"/>
    </UserControl.Resources>

    <StackPanel>
        <Grid Margin="30,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="360"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Email input -->
            <Label Grid.Column="0" Grid.Row="0" Foreground="White" FontWeight="Bold" 
                   Content="Šoupni tady e-mailovou adresu" HorizontalAlignment="Left"/>
            <TextBox Grid.Column="0" Grid.Row="1" Text="{Binding EmailInputString}" HorizontalAlignment="Left" Height="30" Width="230"/>
            <Button Grid.Column="0" Grid.Row="2" Content="Vložit" Width="120" Height="30" 
                    Command="{Binding EmailInputButton}" HorizontalAlignment="Left"/>

            <!-- Last searched emails -->
            <Label Grid.Column="2" Grid.Row="0" Foreground="White" FontWeight="Bold"
                   Content="Seznam posledně vyhledávaných emailových adres" HorizontalAlignment="Left"/>
            <ListBox Grid.Column="2" Grid.Row="1" Grid.RowSpan="4" 
                     ItemsSource="{Binding EmailAddresses}" 
                     SelectedItem="{Binding SelectedEmail}" 
                     Width="360" Height="200" ScrollViewer.VerticalScrollBarVisibility="Auto"/>
           
            <!-- Photo -->
            <Label Grid.Column="0" Grid.Row="3" Foreground="White" FontWeight="Bold"
                Content="Fotografie" HorizontalAlignment="Left"/>
            <Image Grid.Column="0" Grid.Row="4" Width="67" Height="40" Margin="5,5,0,0" HorizontalAlignment="Left"
                Source="{Binding PhotoPath, Converter={StaticResource PhotoPathToUriConverter}}"/>
           
            <!-- Business card text -->
            <Label Grid.Column="0" Grid.Row="6" Grid.ColumnSpan="1" Foreground="White" FontWeight="Bold"
                   Content="Detail kontaktní osoby"/>
            <TextBox Grid.Column="0" Grid.Row="7" Grid.ColumnSpan="3" 
                     Text="{Binding MessageDisplayText}" 
                     Height="150" Width="730" 
                     TextWrapping="Wrap" 
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"/>

            <TextBlock Text="{Binding ErrorMessage}" Grid.Column="0" Grid.Row="11" Grid.ColumnSpan="1"  Foreground="Red" Margin="0,5,0,0"/>
        </Grid>
    </StackPanel>
</UserControl>
